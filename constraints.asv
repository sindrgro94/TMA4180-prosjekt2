function vec = constraints(THETA, angle)
    [n,s] = size(THETA);
    vec_u = zeros(n*s,1);
    vec_l = zeros(n*s,1);
    for i = 1:s*n
        vec_u(i) = angle-THETA(i);
        if vec_u(i) = 
        vec_l(i) = THETA(i) - (-angle);
    end
end